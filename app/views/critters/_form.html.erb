<%= form_for(critter) do |f| %>
  <% if critter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(critter.errors.count, "error") %> prohibited this critter from being saved:</h2>

      <ul>
      <% critter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :color %>
    <%= f.text_field :color %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
