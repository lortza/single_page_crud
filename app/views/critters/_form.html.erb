<%= form_for(critter, remote: true) do |f| %>
  <% if critter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(critter.errors.count, "error") %> prohibited this critter from being saved:</h2>

      <ul>
      <% critter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col-sm-6">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>

    <div class="field col-sm-6">
      <%#= f.label :color %>
      <%#= f.text_field :color, class: 'form-control'  %>
    </div>
  </div> <!-- row -->

  <div class="row">
    <div class="field col-sm-12">
      <%= f.label :city %><br>
      <%= f.collection_radio_buttons :city_id, City.all.order(:name), :id, :name  %>
      <%#= f.collection_select :city_id, City.all.order(:name), :id, :name, class: 'form-control'  %>
    </div>
  </div> <!-- row -->

  <div class="actions">
    <%= f.submit class: 'btn btn-sm btn-success' %>
  </div>
<% end %>
